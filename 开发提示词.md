
执行摘要： 本次迭代首先删除非儿童量表相关代码（如 PHQ-9、GAD-7、SSS 等配置），仅保留儿童核心量表模块。接着开发数据导出脚本（生成 CSV/JSON 并脱敏），新增数据库模型与接口（随访记录、AI 交互日志、随访完成情况等），编写相应单元测试，最终提交 PR。下表给出主要变更与开发任务概览。


删除无关量表文件与代码：

删除 data/scales/ 目录下的非儿童量表 JSON 文件（如下表）及相关引用：
文件/目录	说明	操作
data/scales/phq-9.json	抑郁量表配置 (成人)	删除
data/scales/gad-7.json	焦虑量表配置 (成人)	删除
data/scales/sss.json	躯体化量表配置 (成人)	删除
保留 data/scales/*.json	儿童筛查相关量表配置	无变动
lib/scales/catalog.ts	全量表注册表	同时删去对应条目

在前端筛选逻辑中移除对 exploration 或 adult 类别量表的默认显示支持。
删除医生端/家长端中“探索测试”入口按钮、页面及路由（若有）。
更新界面文案，将“全部”含义限定为所有儿童量表，不再包含成人量表。
提交删除操作并推送：

bash
复制
git add -u data/scales lib/scales catalog.ts src/pages ...
git commit -m "docs: 删除非儿童量表及相关代码，保留儿童核心量表模块"
git push origin backup/pre-delete-$(date +%Y%m%d)
说明：确保 commit 描述清晰，便于审查。

导出并脱敏历史数据：

编写数据导出脚本（Node.js/TS 或 Python），示例如下：
typescript
复制
// 示例: exportData.ts (使用 Prisma Client)
import { PrismaClient } from '@prisma/client';
const prisma = new PrismaClient();
async function exportData() {
  // 导出患儿基线信息
  const children = await prisma.child.findMany({ select: { id: true, sex: true, birthMonth: true, birthYear: true, ... } });
  // 导出测评会话
  const sessions = await prisma.assessmentSession.findMany({ select: { id: true, childId: true, scaleId: true, score: true, riskLevel: true, completedAt: true } });
  // 导出随访情况
  const followups = await prisma.followUp.findMany({ select: { sessionId: true, completedAt: true, status: true } });
  // 整合数据并写入CSV/JSON文件
  // ... (可使用 csv-writer 或 jsonfile 等库)
  console.log("导出完成");
}
exportData();
脱敏规则：将姓名、联系方式、身份证号等字段替换为研究 ID 码；childId 等标识符使用不可逆哈希或随机数替代（确保审计可追溯，但无直接个人信息）。
示例命令：```bash node scripts/exportData.ts > export20230620.json
text
复制
导出后的文件格式建议 CSV 和 JSON 并存，便于不同工具使用。
开发新功能与接口： 按优先级执行开发任务（详见下表）。每项任务完成后应编写单元测试并更新文档。

### 移动端 H5 前端交付范围

本次开发不只改 Next.js 主项目和后端模型，还必须同步改造 `C:\Users\lishuaishuai\Desktop\AIliangbiao\mobile-h5-prototype`。该 H5 是家长自测、医生门诊测评、医生交接填写的真实移动端交付面；详细需求以 `mobile-h5-prototype\移动端H5登录与用户隔离-开发提示词.md` 为子规格。

要求：
- UI、文案、流程贴合“智伴童行 / 儿童发育行为筛查与随访”项目，移除成人量表、探索测试等不相关入口。
- H5 数据源必须来自主项目真实 API、Prisma schema、量表 registry 和 AUS Token 认证，不得用 `mockData`、`mock-aus`、`session-mock` 或随机失败模拟作为完成标准。
- 登录、角色守卫、家长/医生数据隔离、医生交接锁屏、报告/历史/随访入口必须与后端契约联调。
- 若 H5 需要的新接口不存在，先在主项目定义统一接口契约和测试，再改 H5 service；禁止只在 H5 里写假适配。
- 验收必须同时覆盖根项目和 H5 项目构建，并完成移动端 smoke check。

P0 任务（必做）：
任务	目标与要点
随访记录存储	新增后端模型 FollowUp 和前端服务接口；记录家长完成随访时间和状态。
AI交互日志	新增后端模型 AiInteraction；记录用户使用 AI 题目解释/帮助的日志。
报告查看日志	新增后端模型 ReportView; 记录家长查看报告的时间。
数据导出接口	后端提供 /api/research/export 接口，聚合导出清单中的所有衍生表数据。
衍生表建模	基于 Prisma 新建 ChildBaseline, AssessmentSession, ScaleScore, FollowUp, AiInteraction, Outcome3m, InpatientRecord 等表。

P1 任务（次要）：
任务	目标与要点
满意度问卷	前端新增家长/医生满意度调查页面，后端存储满意度数据。
历史数据导入脚本	脚本批量导入旧系统存量量表记录至新数据库，对接 backupDB->Prisma。
接口契约与真实联调	更新 Swagger / Postman 或项目接口契约文档；所有新增接口需完成真实后端联调，Mock 仅限本地 prototype/story，不得作为生产路径或验收依据。

P2 任务（可选）：
任务	目标与要点
自动报表	周期性生成统计报表（如入组量、随访完成率）导出。
多中心支持	架构调整以支持多个医院实例独立部署（机构隔离）。

提交 PR 并填写说明：

先推送所有改动到功能分支（如 feature/research-cleanup-20230620），再发起 Pull Request。PR 描述中注明：
本次变更目的（如“精简量表模块，仅保留儿童相关量表”）。
备份分支名称（如 backup/pre-delete-20230620）。
删除文件清单（见上表）。
数据导出字段清单（见下表）以及脱敏策略。
影响评估：说明该删除如何影响历史对照数据（需要导出后备份），以及新增功能对流程的影响。
回滚方案：如发现问题如何使用备份分支快速恢复。
开发测试与验收标准：

每项新增功能需编写单元测试/集成测试，确保接口正确。
UI 功能需人工验证：随访界面正常弹出，AI 提示功能可用。
导出脚本导出的数据需与数据库一致，并经脱敏验证。
根项目需执行 `rtk npm test` 与 `rtk npm run build`；H5 项目需执行 `rtk npm --prefix mobile-h5-prototype run build`。
移动端 smoke check 需覆盖家长登录、自测、报告；医生登录、选患者、交接填写、PIN 解锁；未授权访问医生页会被拦截。
静态检查需确认 H5 生产路径不再依赖 `mockAuthUsers`、`mockData`、`mock-aus`、`session-mock`。
PR 审核建议由数据管理员和伦理负责人共同完成，确保合规。
B. 项目负责人/科室说明（正式文档风格）
本次开发计划围绕《中国医院协会2026年健康促进项目》的研究需求，实施以下主要改动与建设：为建立儿童发育行为筛查与随访的健康促进闭环，我们在现有 AI 量表系统基础上精简量表模块，仅保留儿童相关量表，对成人/探索量表部分进行删除或归档。同时新增数据采集与分析功能，包括随访记录和 AI 交互日志等衍生表，为后续效果评估提供完整数据支撑。本方案已考虑数据安全与伦理审批，具体说明如下。

变更内容摘要：

删除内容： 移除配置化量表目录下的成人量表文件（PHQ-9、GAD-7、SSS）及相关注册与界面入口；保留对儿童筛查核心量表的支持。
新增内容： 扩展后端数据库模型，新增“随访记录（FollowUp）”、“AI 交互日志（AiInteraction）”、“报告查看（ReportView）”等数据表；提供数据导出接口，便于研究团队下载脱敏数据。
迁移与备份： 已在 GitHub 建立 backup/pre-delete-YYYYMMDD 分支，完整保存变更前代码；所有旧数据在导出脚本执行后保存在安全存储中，确保随时可恢复。
开发计划与任务分配：

技术团队（后台/前端）：负责代码变更、接口开发与测试，预计 3 个月内完成。
数据管理员：负责旧数据导出和数据库迁移，确保历史病例信息完整且脱敏存档。
统计/科研人员：参与需求确认与衍生表设计，定义导出字段；后续进行数据分析。
伦理/安全负责人：审核个人信息脱敏方案，确保新功能符合隐私政策和医院伦理规范。
项目协调：提交开发进度汇报，组织定期评审会议，确保跨部门沟通顺畅。
需导出字段清单：
为保存历史对照数据与后续分析，需导出以下主要数据表并脱敏处理：

表名	字段示例	说明
child_baseline	childId(脱敏码), birthYear, sex, city, chiefComplaint…	儿童基本信息（出生年月、性别等）
assessment_session	sessionId, childId, scaleId, totalScore, riskLevel, completedAt, fillMode…	每次测评会话记录
scale_score	sessionId, questionId, answer, score…	单次测评各题目回答详情
ai_interaction	sessionId, questionId, askedAt, answeredByAI…	AI 助手题目解释使用记录
followup	sessionId, reminderSentAt, reminderClickedAt, completedAt…	家长随访响应与完成情况
inpatient_record	admissionId, childId, admissionDate, treatmentType…	住院干预记录（如有）
outcome_3m	childId, threeMonthScoreChange, interventionStarted…	3 个月随访结果和是否启动干预

说明：childId 等标识已由原始 ID 映射至研究用随机 ID，确保不能反查个人信息。

安全与合规注意：

伦理审批：所有变更已在项目立项阶段提交伦理委员会审批，获批后方可实施；后续数据导出和使用均在知情同意范围内进行。
脱敏存储：导出数据中个人隐私字段（姓名、身份证、联系方式）均替换为研究 ID；敏感信息仅在安全服务器上以加密形式保留，且访问受限。
备份与保留：变更前代码与数据库备份将长期保留在备份分支和安全存储中，定期审核备份有效性；生产环境新数据保留策略同医院数据管理规范。
访问控制：新增功能的代码和数据接口仅限项目组和相关安全审核人员访问；GitHub PR 审核建议至少包含一名数据安全负责人和研发负责人双重签字。
风险与回滚：

删除配置化量表后，若发现系统误删或后续有分析需求，可通过 backup/pre-delete-YYYYMMDD 分支快速恢复相关文件与代码。历史数据导出前将进行校验，确保无丢失或缺失；若数据不全可重新执行导出脚本。
新增功能上线前将进行灰度测试，医生端及家长端分别测试量表评估和随访流程，确保无异常。
成果产出：

完成 科研数据表 和 导出文件，支持论文研究和目标试验模拟。
发布 儿童发育行为筛查健康促进闭环流程规范 文档，用于后续医院推广。
形成系统 软件著作权 申请材料。
提交 项目结题报告 和 学术论文，展示改进效果与因果验证结果。
附件表格：

